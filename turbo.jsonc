{
	"$schema": "https://turborepo.com/schema.json",
	"globalDependencies": [
		// Many packages extend root TS configs; changing them must invalidate caches.
		"tsconfig*.json",
		// Workspace-level Vitest entry (used by `pnpm test:full` at repo root).
		"vitest.config.ts",
		"pnpm-workspace.yaml"
	],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", "public/**"]
		},
		"test": {
			"outputs": []
		},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
