# @pluxel/promptkit

放纯函数风格、与插件生命周期无关的“提示词/LLM 工具函数”：

- `@pluxel/promptkit/toon`：TOON 编解码 + `formatStructured()`（json/toon 显式选择）
- `@pluxel/promptkit/mdream`：再导出 `mdream`（HTML → Markdown），并提供常用子路径导出（plugins/preset）
