{
	"extends": "./tsconfig.base.json",
	"compilerOptions": {
		"noEmit": true,
		"composite": false,
		"declaration": false,
		"declarationMap": false,
		// Host entry should typecheck against dist types to avoid pulling workspace plugin sources into this program.
		"customConditions": []
	},
	"include": ["src/**/*.ts", "src/**/*.tsx", "vitest.config.ts"]
}
